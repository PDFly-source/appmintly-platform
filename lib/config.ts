// APPFORGE Global Configuration
// Production Mode: SHOW ONLY REAL PUBLISHED CATALOG APPS.
// Demo Mode: May show sample applications.
// Default production configuration: false (no demo apps leak into public pages).
export const APPFORGE_DEMO_MODE = false;

// Number of days an app is considered "New" after publication/release
export const NEW_APP_THRESHOLD_DAYS = 30;

// Maximum number of items in featured carousel fallback
export const DEFAULT_FEATURED_LIMIT = 5;
